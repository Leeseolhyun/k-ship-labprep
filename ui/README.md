# BlockFlow UI

API 없이 동작하는 정적 데모 UI입니다. 기존 데이터 생성 및 GA 코드와 독립적으로 실행됩니다.

```powershell
docker compose up --build -d
```

브라우저에서 http://localhost:3000 을 엽니다. 같은 네트워크의 구성원은 호스트 PC의 IPv4 주소에 `:3000`을 붙여 접속할 수 있습니다.

자세한 이미지 공유 방법은 [DOCKER.md](DOCKER.md)를 확인하세요.
